// Enumeration

#include <iostream>
#include <math.h>

using namespace std;

int main() {
    int n;
    
    cin >> n;
    // print n^2 integers as a triangle with its base at the left, of length 2N-1. Every integer will be greater than the integer on the left by 1 and it will be greater than the integers above.
    
    return 0;
}